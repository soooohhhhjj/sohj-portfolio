{
  "name": "sohj-portfolio-next",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev:client": "npm --workspace client run dev",
    "build": "npm --workspace client run build"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.0"
  }
}
